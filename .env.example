# Public Federation API used by the widget and agent examples.
FEDERATION_GATEWAY=https://fapi.drdeeks.xyz
FEDERATION_PROJECT=autopilot

# Keep deployment credentials outside the repository.
# CLOUDFLARE_API_TOKEN=

# Local development only. In production, set these with `wrangler secret put`.
# WATCHTOWER_INGESTION_SECRET=
# WATCHTOWER_ADMIN_TOKEN=
